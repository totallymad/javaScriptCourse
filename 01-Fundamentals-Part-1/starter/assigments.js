/* const country = 'Russian Federation',
      isIsland = false,
      language = 'russian';

let population = 146150789;


console.log(country);
console.log(population);
console.log(isIsland);
console.log(language);

console.log(population / 2);
// population++;
console.log(population);
let isPopulationLargeThanFin = population > 6000000;
console.log(isPopulationLargeThanFin);

let averagePopulation = 33000000;
let isPopulationLargeThanAverage = population > averagePopulation;

console.log(isPopulationLargeThanAverage);

console.log(country + ' is on Europe and Asia, and its ' + population + ' peaople speak ' + language);

const description = `${country} is on Europe and Asia, and its ${population} peaople speak ${language}`;

console.log(description);

if (population > averagePopulation) {
    console.log(`Население ${country} выше среднего`);
} else if (population < averagePopulation) {
    console.log(`Население ${country} на ${averagePopulation - population} меньше среднего`);
} else {
    console.log(`Население ${country} равно среднему`);
}

console.log(population > averagePopulation ? 'Население России выше среднего' : 'Численность населения России ниже среднего'); */


/* let numNeighbours = +prompt('Сколько соседних стран у вашей страны есть?');

if (numNeighbours === 1) {
    console.log('Только 1 граница!')
} else if (numNeighbours > 1) {
    console.log('Более 1 границы')
} else {
    console.log('Нет границы')
} */

/* const language = 'Russian'

switch (language) {
    case 'Chinese':
        console.log('Наибольшее количество носителей языка')
        break;
    case 'Indian':
        console.log("2-е место по количеству носителей языка")
        break;
    case 'English':
        console.log("3-e место");
        break;
    case 'Hindi':
        console.log('Номер 4');
        break;
    case 'Arabian':
        console.log('5-й по распространнености язык');
        break;
    default:
        console.log('Тоже отличный язык😊')
}

 */